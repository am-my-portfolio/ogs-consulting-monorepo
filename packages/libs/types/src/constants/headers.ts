export enum Environment {
  LOCAL = 'local',
  DEV = 'dev',
  PROD = 'prod',
}

export enum CustomHeader {
  X_CUSTOM_HEADER_EXAMPLE = 'x-custom-header',
  X_APP_NAME = 'x-COMPANY-NAME-appp-name',
}

export enum StandardHeader {
  AUTHORIZATION = 'authorization',
  CONTENT_TYPE = 'content-type',
  USER_AGENT = 'user-agent',
}

export enum AudienceType {
  Business = 'Business',
  Customer = 'Customer',
  Driver = 'Driver',
  Franchise = 'Franchise',
  Investor = 'Investor',
}
